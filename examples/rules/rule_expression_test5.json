{
  "metadata": {
    "created": "2023-03-29",
    "priority": 10,
    "test purpose": "check that the inequality on the missing value age evaluates to true, also test isEqualToAny()"
  },
  "condition": {
    "expression": "!hasValue(ame) && hasValue(name) && name == \"Frank\"  && !(age == 20) && isEqualToAny(gender, 1, 2, 3, \"male\") && age != 10 && !isEqualToAny(age, 1,2,3)"
  }
}