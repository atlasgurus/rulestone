- metadata:
    created: "2023-03-29"
    priority: 10
    test purpose: "check that missing age field with undefined semantics (age == undefined), also test isEqualToAny()"
  expression: '!hasValue(ame) && hasValue(name) && name == "Frank" && age == undefined && isEqualToAny(gender, 1, 2, 3, "male")'
